// Placeholder for Task 16, which replaces this with the real dialog (template
// picker, declared-variable form, server-rendered preview, generate). Task 15's own
// tests do not open it; this only keeps DocumentsTab.tsx type-checking in the
// interim.
export function NewFromTemplateDialog(props: Record<string, unknown>) {
  void props
  return null
}
